<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="shop.fims.account.CommitteeMapper">

	<!-- 모든 위원회 관리 -->
	  <select id="selectAllCommitteeView" resultType="shop.fims.vo.Committee">
	  	SELECT
			*
		FROM 
			Man_Com
	  </select>

</mapper>